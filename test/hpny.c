#include <stdio.h>
#include <stdlib.h>

int M;
int a, e, h, m, p, r, w, x, y;

int solve()
{
  int result;
  h = 1;
  for (h = 1; h <= 2; h ++)
    {
      for (a = 0; a <= 9; a ++)
	{
	  if (a == h)
	    continue;
	  else
	    {
	      for
	    }
	}
    }
}
int main
{
  scanf("%d\n", &N);
  
}
