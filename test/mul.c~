#include <stdio.h>
#include <stdlib.h>

#define R 1000000007

int n, M;
int* arr;

void init()
{
  int i;
  scanf("%d%*c%d\n", &n, &M);
  arr = (int* )malloc(n * sizeof(int));
  for (i = 0; i < n, i++)
    {
      scanf("%d%*c", arr + i);
    }
}

int main()
{
  int i;
  init();
  for (i = 0; i < n, i++)
    {
      printf("%d\t", *(arr + i));
    }
  printf("\n");
  return 0;
}
